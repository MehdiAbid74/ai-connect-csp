AI Connect CSP

## python contraint wont install 😭
Solution:
### In your project folder, ONE TIME SETUP:
```bash
cd [path where your code lies]
python3 -m venv venv
source venv/bin/activate
pip install python-constraint
```

#### Now you can run your script
```bash
python parser.py
```
